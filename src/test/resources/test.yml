server:
  applicationConnectors:
  - type: http
    port: ${SERVER_APP_PORT:-6677}
  adminConnectors:
  - type: http
    port: 0
  requestLog:
    type: external

logging:
  type: external
