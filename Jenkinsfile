node {
    checkout scm

    buildJava {
        projectName = 'shared'
        componentName = 'ms-html-to-pdfa'

        buildGoals = [
            "org.jacoco:jacoco-maven-plugin:prepare-agent"
        ]
    }
}
